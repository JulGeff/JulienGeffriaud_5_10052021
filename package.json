{
  "name": "orinoco-backend",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass": "sass --watch ./assets/sass/main.scss:./assets/css/style.css --style compressed",
    "prefix": "postcss ./assets/css/style.css --use autoprefixer -d ../assets/css/prefixed/"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.4",
    "uuid": "^3.3.2"
  },
  "devDependencies": {
    "eslint": "^7.28.0"
  }
}
